import Container from "./components/Content";

const About = () => {
return (
	<Container text="The Conjugate Method Workout Generator is a web browser-based application for powerlifters. The application generates a randomized workout of the day (WOD) for the user. Each workout will have one main lift and 4 accessory lifts. The workouts and the max-effort records set by the user will be saved in the user’s individual account. The workouts will be modeled after the Conjugate Method as popularized by the late Louie Simmons of Westside Barbell."/>
);
};

export default About;
